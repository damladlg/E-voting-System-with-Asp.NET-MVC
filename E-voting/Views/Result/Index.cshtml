@model IEnumerable<E_voting.Models.Model.Result>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@*@model IEnumerable<KurumsalWeb.Models.Model.Kategori>
    <ul class="cat">
        @foreach (var item in Model)
        {
            <li><i class="icon-angle-right"></i><a href="/BlogPost/@item.KategoriId">@item.KategoriAd</a><span> (@item.Blogs.Count())</span></li>
        }
    </ul>*@

<div class="row">
    @ViewBag.Message
</div>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CandidateId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VoterId)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CandidateId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VoterId)
            </td>
            @*<td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.VoteCastingId }) |
                    @Html.ActionLink("Details", "Details", new { id=item.VoteCastingId }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.VoteCastingId })
                </td>*@
        </tr>
    }

</table>
